services:
  - type: web
    name: jsl
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: node dist/app/server/server.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.16.0
